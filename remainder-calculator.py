"""
Python program to find the remainder when one number is divided by another
"""

dividend = float(input("Enter your dividend: "))
divisor = float(input("Enter your divisor: "))

remainder = dividend % divisor

print(f"The remainder after {dividend} was divided by {divisor} is {remainder}")
